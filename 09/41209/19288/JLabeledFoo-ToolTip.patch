diff -ru src/jorphan.orig/org/apache/jorphan/gui/JLabeledChoice.java src/jorphan/org/apache/jorphan/gui/JLabeledChoice.java
--- src/jorphan.orig/org/apache/jorphan/gui/JLabeledChoice.java	2005-10-26 01:07:54.000000000 +0200
+++ src/jorphan/org/apache/jorphan/gui/JLabeledChoice.java	2006-12-19 12:58:53.000000000 +0100
@@ -266,4 +266,24 @@
 			}
 		}
 	}
-}
\ No newline at end of file
+
+        /**
+	 * Registers the text to display in a tool tip.
+	 * The text displays when the cursor lingers over the component.
+	 * @param text the string to display; if the text is null,
+	 *      the tool tip is turned off for this component
+	 */
+	public void setToolTipText(String text) {
+		choiceList.setToolTipText(text);
+	}
+
+	/**
+	  * Returns the tooltip string that has been set with setToolTipText
+	  * @return the text of the tool tip
+	  */
+	public String getToolTipText() {
+		return choiceList.getToolTipText();
+	}
+
+
+}
diff -ru src/jorphan.orig/org/apache/jorphan/gui/JLabeledTextArea.java src/jorphan/org/apache/jorphan/gui/JLabeledTextArea.java
--- src/jorphan.orig/org/apache/jorphan/gui/JLabeledTextArea.java	2005-07-12 22:51:09.000000000 +0200
+++ src/jorphan/org/apache/jorphan/gui/JLabeledTextArea.java	2006-12-19 12:56:20.000000000 +0100
@@ -202,4 +202,23 @@
 			((ChangeListener) mChangeListeners.get(index)).stateChanged(ce);
 		}
 	}
-}
\ No newline at end of file
+
+	/**
+	 * Registers the text to display in a tool tip.
+	 * The text displays when the cursor lingers over the component.
+	 * @param text the string to display; if the text is null,
+	 *      the tool tip is turned off for this component
+	 */
+	public void setToolTipText(String text) {
+		mTextArea.setToolTipText(text);
+	}
+
+	/**
+	  * Returns the tooltip string that has been set with setToolTipText
+	  * @return the text of the tool tip
+	  */
+	public String getToolTipText() {
+		return mTextArea.getToolTipText();
+	}
+
+}
diff -ru src/jorphan.orig/org/apache/jorphan/gui/JLabeledTextField.java src/jorphan/org/apache/jorphan/gui/JLabeledTextField.java
--- src/jorphan.orig/org/apache/jorphan/gui/JLabeledTextField.java	2006-12-19 12:48:53.000000000 +0100
+++ src/jorphan/org/apache/jorphan/gui/JLabeledTextField.java	2006-12-19 12:58:54.000000000 +0100
@@ -210,4 +210,22 @@
 		}
 	}
 
+        /**
+	 * Registers the text to display in a tool tip.
+	 * The text displays when the cursor lingers over the component.
+	 * @param text the string to display; if the text is null,
+	 *      the tool tip is turned off for this component
+	 */
+	public void setToolTipText(String text) {
+		mTextField.setToolTipText(text);
+	}
+
+	/**
+	  * Returns the tooltip string that has been set with setToolTipText
+	  * @return the text of the tool tip
+	  */
+	public String getToolTipText() {
+		return mTextField.getToolTipText();
+	}
+
 }
Only in src/jorphan/org/apache/jorphan/gui: JLabeledTextField.java.new
Only in src/jorphan/org/apache/jorphan/gui: JLabeledTextField.java.orig
