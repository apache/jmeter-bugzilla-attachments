					Changes Made in ProxyControlGui.java


1.ProxyControlGui now implements ItemListener

	public class ProxyControlGui
   	 extends LogicControllerGui
   	 implements
        	JMeterGUIComponent,
        	ActionListener,
        	KeyListener,
        	FocusListener,
        	UnsharedComponent,
        	ItemListener

2.In actionPerformed (ActionEvent action) method commented the following statements:

		else if ( command.equals (ProxyControl.CAPTURE_HTTP_HEADERS)
			|| command.equals (ProxyControl.ADD_ASSERTIONS)
            		|| command.equals (ProxyControl.GROUPING_MODE)
            		|| command.equals (ProxyControl.USE_KEEPALIVE)
                 
        		{
            		enableRestart ();
        		} 

3.In createPortPanel () method commented the following statements:

		httpHeaders.addActionListener (this);
		httpHeaders.setActionCommand (ProxyControl.CAPTURE_HTTP_HEADERS);

		addAssertions.addActionListener (this);
		addAssertions.setActionCommand (ProxyControl.ADD_ASSERTIONS);

		useKeepAlive.addActionListener (this);
		useKeepAlive.setActionCommand (ProxyControl.USE_KEEPALIVE);

4.In createPortPanel () method added the following statements:

		httpHeaders.addItemListener (this);
		addAssertions.addItemListener (this);
		useKeepAlive.addItemListener (this);

5.In createGroupingPanel () method commented the following statements:

		groupingMode.addActionListener (this);
		groupingMode.setActionCommand (ProxyControl.GROUPING_MODE);

6.In createGroupingPanel () method added the following statement:

		groupingMode.addItemListener (this);

7.Added the following method in ProxyControlGui.java:

		public void itemStateChanged (ItemEvent e)
 		{
        		enableRestart();
  		}


