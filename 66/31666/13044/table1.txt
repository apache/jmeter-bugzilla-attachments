CREATE TABLE `testresults`.`test` (
  `LABEL` CHAR(50) NOT NULL,
  `THREADNAME` CHAR(50) NOT NULL,
  `RESPONSECODE` CHAR(5),
  `CONTENTTYPE` CHAR(40),
  `CONTENTLENGTH` BIGINT UNSIGNED,
  `STARTTIME` TIMESTAMP NOT NULL,
  `ENDTIME` TIMESTAMP NOT NULL,
  `SAMPLETIME` BIGINT UNSIGNED NOT NULL,
  `idx` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY
);
